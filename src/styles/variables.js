import { css } from "styled-components";

const variables = css`
  :root {
    /* colors */
    --black: rgba(37, 36, 35, 1);
    --grey: rgba(174, 187, 207, 1);
    --orange: rgba(243, 85, 34, 1);

    --font: "Telegraf", sans-serif;
  }
`;

export default variables;
